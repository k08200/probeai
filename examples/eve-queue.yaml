id: eve-queue-list
name: "EVE Queue List"
description: "Verify EVE can list the task queue"

agent:
  type: command
  command: "cd /Users/yongrean/Downloads/nightbot && npx tsx src/cli.ts queue"

steps:
  - action: check_output
    expect: "queue"

evaluate:
  method: rules
  passThreshold: 50
  rules:
    - type: exit_code
      target: exit
      value: "0"
      weight: 2
    - type: regex
      target: stdout
      value: "(pending|running|done|No tasks|Queue)"
      weight: 1
